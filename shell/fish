#export LC_ALL=C.UTF-8
#export LANG=C.UTF-8
if set -q MSYSTEM
    source /d/vimconfig/shell/setting.fish
else
    source ~/vimconfig/shell/setting.fish
end

#-- alias
alias lg lazygit
alias bat='bat --style="header" --paging=never --theme=TwoDark'
#alias ls 'lsd --icon never'
#alias ll 'lsd --icon never -all'
alias ls lsd
alias ll 'lsd -all'
alias cls reset

#-- export

set -x EDITOR 'vim -u NONE'
set -x VISUAL 'vim -u NONE'

#  archlinux  pkgfile --update  Debian update-command-not-found
